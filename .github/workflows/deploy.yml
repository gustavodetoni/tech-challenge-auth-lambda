name: Deploy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Deploy Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: echo "Deploy disabled for the first GitHub version. Enable this workflow after AWS secrets and Terraform backend are configured."

# Deploy futuro:
#
# on:
#   push:
#     branches:
#       - homolog
#       - main
#
# jobs:
#   deploy:
#     name: Deploy Lambda
#     runs-on: ubuntu-latest
#     environment: ${{ github.ref_name == 'main' && 'production' || 'homolog' }}
#     steps:
#       - uses: actions/checkout@v6
#       - uses: actions/setup-go@v6
#         with:
#           go-version-file: go.mod
#       - run: make tests
#       - run: make package
#       - uses: hashicorp/setup-terraform@v3
#       - run: terraform -chdir=terraform init -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}"
#       - run: terraform -chdir=terraform plan -out=tfplan
#       - run: terraform -chdir=terraform apply -auto-approve tfplan
